#!/bin/sh
# Return locally mirrored comments for a post path through the Gazeta Nostr read runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-nostr-read-runtime-adapter" blog-comments

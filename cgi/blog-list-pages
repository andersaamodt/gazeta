#!/bin/sh
# List available curated list pages through the Gazeta authenticated admin read runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-admin-read-runtime-adapter" blog-list-pages

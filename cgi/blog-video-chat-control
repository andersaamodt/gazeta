#!/bin/sh
# Handle video chat control actions through the Gazeta Theurgy admin runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-admin-runtime-adapter" blog-video-chat-control

#!/bin/sh
# Handle Secure Chat admin actions through the Gazeta Theurgy admin runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-admin-runtime-adapter" blog-secure-chat-admin

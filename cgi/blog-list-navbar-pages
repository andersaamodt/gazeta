#!/bin/sh
# Public navbar page list through the Gazeta Theurgy read runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-read-runtime-adapter" list-navbar-pages

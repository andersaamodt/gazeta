#!/bin/sh
# Shared CGI adapter wrapper for Gazeta Theurgy authenticated admin read-runtime actions.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-runtime-adapter" admin-read "${1-}"

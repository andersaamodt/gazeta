#!/bin/sh
# Return normalized product metadata through the Gazeta commerce read runtime.

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd -P)
exec /bin/sh "$SCRIPT_DIR/gazeta-commerce-read-runtime-adapter" blog-get-product
